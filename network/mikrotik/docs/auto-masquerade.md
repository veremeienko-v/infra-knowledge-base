## Автоматичне додавання правила masquerade:

Скопіювати скрипт з [файлу](../scripts/auto-masquerade.rsc), до DHCP Client (наприклад ISP1) в мікротіку. Скрипт автоматично додає (при статусі Bound=1) та видаляє (при неактивнову DHCP клієнті) правило masquerade.
